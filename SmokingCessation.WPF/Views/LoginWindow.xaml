<Window x:Class="SmokingCessation.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="600" 
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Grid.Row="0" 
                                  Mode="PrimaryMid" 
                                  Padding="16">
            <StackPanel>
                <materialDesign:PackIcon Kind="SmokeFree" 
                                       Width="64" 
                                       Height="64" 
                                       HorizontalAlignment="Center"
                                       Foreground="White"/>
                <TextBlock Text="Smoking Cessation Platform" 
                          Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                          HorizontalAlignment="Center"
                          Foreground="White"
                          Margin="0,8,0,0"/>
                <TextBlock Text="Your journey to a smoke-free life starts here" 
                          Style="{StaticResource MaterialDesignBody2TextBlock}"
                          HorizontalAlignment="Center"
                          Foreground="White"
                          Opacity="0.87"/>
            </StackPanel>
        </materialDesign:ColorZone>

        <!-- Login Form -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Padding="32">
            <StackPanel MaxWidth="300">
                
                <TextBlock Text="Sign In" 
                          Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,32"/>

                <!-- Error Message -->
                <materialDesign:Card Background="{DynamicResource MaterialDesignValidationErrorBrush}"
                                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                                   Margin="0,0,0,16">
                    <TextBlock Text="{Binding ErrorMessage}"
                              Foreground="White"
                              Padding="16,8"
                              TextWrapping="Wrap"/>
                </materialDesign:Card>

                <!-- Email -->
                <TextBox materialDesign:HintAssist.Hint="Email Address"
                         materialDesign:HintAssist.IsFloating="True"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Margin="0,0,0,20">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <!-- Password -->
                <PasswordBox materialDesign:HintAssist.Hint="Password"
                           materialDesign:HintAssist.IsFloating="True"
                           x:Name="PasswordBox"
                           Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                           Margin="0,0,0,20">
                    <PasswordBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                    </PasswordBox.InputBindings>
                </PasswordBox>

                <!-- Remember Me -->
                <CheckBox Content="Remember me"
                         IsChecked="{Binding RememberMe}"
                         Margin="0,0,0,24"/>

                <!-- Login Button -->
                <Button Command="{Binding LoginCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       materialDesign:ButtonAssist.CornerRadius="20"
                       Height="40"
                       Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Login" 
                                               Margin="0,0,8,0"
                                               Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
                        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                   Width="16"
                                   Height="16"
                                   Margin="0,0,8,0"
                                   Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                                   IsIndeterminate="True"/>
                        <TextBlock Text="{Binding IsBusy, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Signing in...|Sign In'}"/>
                    </StackPanel>
                </Button>

                <!-- Forgot Password -->
                <Button Command="{Binding ForgotPasswordCommand}"
                       Style="{StaticResource MaterialDesignFlatButton}"
                       Content="Forgot Password?"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,24"/>

                <!-- Divider -->
                <Separator Margin="0,0,0,24"/>

                <!-- Register -->
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Center">
                    <TextBlock Text="Don't have an account?"
                              VerticalAlignment="Center"
                              Margin="0,0,8,0"/>
                    <Button Command="{Binding RegisterCommand}"
                           Style="{StaticResource MaterialDesignFlatButton}"
                           Content="Sign Up"
                           Padding="8,4"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <materialDesign:ColorZone Grid.Row="2" 
                                  Mode="PrimaryDark" 
                                  Padding="16,8">
            <TextBlock Text="© 2024 Smoking Cessation Platform. All rights reserved." 
                      Style="{StaticResource MaterialDesignCaptionTextBlock}"
                      HorizontalAlignment="Center"
                      Foreground="White"
                      Opacity="0.7"/>
        </materialDesign:ColorZone>

    </Grid>
</Window>